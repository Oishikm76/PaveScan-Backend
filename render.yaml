# ---------- render.yaml ----------


render_yaml = """
services:
- type: web
name: pavescan-backend
env: docker
plan: free
dockerCommand: uvicorn main:app --host 0.0.0.0 --port 10000
autoDeploy: true
"""


with open("render.yaml", "w") as f:
f.write(render_yaml)
